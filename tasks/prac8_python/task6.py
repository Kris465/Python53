import random


def start_game(min_num, max_num, attempts):
    """
    Запускает игру 'Угадай число' с заданными настройками

    Args:
        min_num: минимальное число диапазона
        max_num: максимальное число диапазона
        attempts: количество попыток

    Returns:
        True если пользователь угадал, False если проиграл
    """
    # Генерируем случайное число в заданном диапазоне
    secret_number = random.randint(min_num, max_num)

    print(f"Угадайте число от {min_num} до {max_num}.")
    print(f"У вас {attempts} попыток.\n")

    # Цикл по количеству попыток
    for attempt in range(attempts):
        try:
            # Запрашиваем предположение пользователя
            guess = int(input(f"Попытка {attempt + 1}: "))

            # Проверяем предположение
            if guess == secret_number:
                print("🎉 Поздравляем! Вы угадали!")
                return True
            elif guess < secret_number:
                print("🔺 Загаданное число БОЛЬШЕ")
            else:
                print("🔻 Загаданное число МЕНЬШЕ")

        except ValueError:
            # Обрабатываем случай, когда пользователь ввел не число
            print("❌ Пожалуйста, введите целое число")

    # Если цикл завершился - пользователь исчерпал все попытки
    print(f"\n💔 Вы проиграли! Загаданное число было: {secret_number}")
    return False


if __name__ == "__main__":
    print("=== Игра 'Угадай число' ===\n")

    # Запускаем игру с настройками: от 1 до 50, 5 попыток
    # Закомментируйте следующую строку для автоматического тестирования
    start_game(1, 50, 5)

    # Для автоматического тестирования (без ввода пользователя):
    # print("Для тестирования раскомментируйте код в файле")
