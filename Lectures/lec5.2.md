### **Лекция 5.2: SQL: Оператор SELECT**

**Цель лекции:** Научиться извлекать данные из таблиц с помощью оператора `SELECT`, фильтровать их, сортировать и ограничивать результаты.

#### **Теоретическая часть**

**1. Оператор `SELECT` — основа извлечения данных**
Это самый часто используемый оператор SQL. Он позволяет читать данные из таблицы, не изменяя их.

**Базовый синтаксис:**
```sql
SELECT столбец1, столбец2, ... 
FROM имя_таблицы;
```
*   `SELECT *` — выбор **всех** столбцов таблицы.
*   Можно выбирать конкретные столбцы через запятую.
*   `FROM` указывает, из какой таблицы выбирать данные.

**2. Фильтрация строк: `WHERE`**
Позволяет выбрать только те строки, которые удовлетворяют заданному условию.

```sql
SELECT * 
FROM students 
WHERE age > 20;
```

**Операторы сравнения:**
*   `=`, `<>` или `!=` (не равно)
*   `<`, `>`, `<=`, `>=`
*   `BETWEEN` (между диапазоном значений)
*   `LIKE` (поиск по шаблону, `%` — любое количество символов, `_` — один символ)
*   `IN` (проверка вхождения в список)

**Логические операторы:**
*   `AND` — оба условия истинны
*   `OR` — хотя бы одно условие истинно
*   `NOT` — отрицание условия

**3. Сортировка: `ORDER BY`**
Упорядочивает результат по указанному столбцу.
```sql
SELECT * 
FROM students 
ORDER BY last_name ASC; -- по возрастанию (по умолчанию)
```
*   `ASC` — по возрастанию (A→Z, 0→9)
*   `DESC` — по убыванию (Z→A, 9→0)
*   Можно сортировать по нескольким столбцам: `ORDER BY group_name ASC, last_name DESC`

**4. Ограничение вывода: `LIMIT`**
Ограничивает количество возвращаемых строк. Полезно для пагинации или просмотра первых N записей.
```sql
SELECT * 
FROM students 
LIMIT 5; -- только первые 5 записей
```
*   `LIMIT 10 OFFSET 5` — пропустить 5 записей, показать следующие 10 (для постраничного вывода).

#### **Практическая часть: Работа с данными через SQL и Python**

**Подготовка данных:**
1. Откройте DB Browser for SQLite и свою базу `university.db` из предыдущей лекции.
2. Если таблица `students` пуста, добавьте в неё следующие данные через вкладку "Выполнить SQL":

```sql
INSERT INTO students (first_name, last_name, age, group_name) VALUES
('Анна', 'Иванова', 20, 'ИТ-101'),
('Петр', 'Смирнов', 22, 'ИТ-101'),
('Мария', 'Кузнецова', 19, 'Л-201'),
('Иван', 'Попов', 21, 'ИТ-101'),
('Елена', 'Соколова', 23, 'Л-201'),
('Алексей', 'Лебедев', 18, 'Ф-301'),
('Ольга', 'Козлова', 20, 'ИТ-101'),
('Дмитрий', 'Новиков', 24, 'Ф-301'),
('Наталья', 'Морозова', 19, 'Л-201'),
('Сергей', 'Петров', 22, 'ИТ-101');
```

**Задание 1: Чистый SQL (выполняем в DB Browser)**

Выполните следующие запросы во вкладке "Выполнить SQL" и проанализируйте результаты:

1. **Базовый SELECT:**
```sql
-- 1. Выберите всех студентов
SELECT * FROM students;

-- 2. Выберите только имена и фамилии студентов
SELECT first_name, last_name FROM students;
```

2. **Фильтрация с WHERE:**
```sql
-- 3. Студенты старше 21 года
SELECT * FROM students WHERE age > 21;

-- 4. Студенты из группы ИТ-101
SELECT * FROM students WHERE group_name = 'ИТ-101';

-- 5. Студенты ИТ-101, младше 22 лет (AND)
SELECT * FROM students 
WHERE group_name = 'ИТ-101' AND age < 22;

-- 6. Студенты из групп ИТ-101 или Л-201 (OR, IN)
SELECT * FROM students 
WHERE group_name = 'ИТ-101' OR group_name = 'Л-201';

-- То же через IN:
SELECT * FROM students 
WHERE group_name IN ('ИТ-101', 'Л-201');

-- 7. Студенты, чья фамилия начинается на "С" (LIKE)
SELECT * FROM students 
WHERE last_name LIKE 'С%';
```

3. **Сортировка и ограничение:**
```sql
-- 8. Отсортировать студентов по возрасту (от младшего к старшему)
SELECT * FROM students ORDER BY age;

-- 9. Отсортировать по группе, а внутри группы по фамилии
SELECT * FROM students 
ORDER BY group_name, last_name;

-- 10. Показать 3 самых старших студента
SELECT * FROM students 
ORDER BY age DESC 
LIMIT 3;

-- 11. Пропустить первых 2 студентов, показать следующие 4
SELECT * FROM students 
LIMIT 4 OFFSET 2;
```

**Задание 2: Python-скрипт для выполнения SQL-запросов**

Создайте файл `student_queries.py` и напишите скрипт, который:
1. Подключается к базе данных `university.db`
2. Выполняет сложные запросы с фильтрацией
3. Выводит результаты в читаемом формате

```python
import sqlite3
from tabulate import tabulate  # для красивого вывода таблиц

# Установите библиотеку: pip install tabulate

def execute_query(sql: str) -> list:
    """Выполняет SQL запрос и возвращает результат"""
    with sqlite3.connect('university.db') as conn:
        cursor = conn.cursor()
        cursor.execute(sql)
        result = cursor.fetchall()
        # Получаем названия столбцов
        column_names = [description[0] for description in cursor.description]
        return column_names, result

def print_query_result(title: str, sql: str):
    """Красиво выводит результат запроса"""
    print(f"\n{'='*50}")
    print(f"{title}")
    print(f"SQL: {sql}")
    print('='*50)
    
    column_names, data = execute_query(sql)
    
    if data:
        print(tabulate(data, headers=column_names, tablefmt="grid"))
    else:
        print("Нет данных")

# Основные запросы для выполнения
queries = [
    ("1. Все студенты", "SELECT * FROM students"),
    
    ("2. Студенты ИТ-101, отсортированные по имени", 
     "SELECT first_name, last_name, age FROM students WHERE group_name = 'ИТ-101' ORDER BY first_name"),
    
    ("3. Самые молодые студенты из каждой группы (первые 2 из каждой)",
     "SELECT group_name, first_name, last_name, MIN(age) as min_age FROM students GROUP BY group_name ORDER BY group_name LIMIT 2"),
    
    ("4. Студенты, чьи фамилии заканчиваются на 'ов' или 'ова'",
     "SELECT * FROM students WHERE last_name LIKE '%ов' OR last_name LIKE '%ова' ORDER BY last_name"),
    
    ("5. Статистика по группам (количество студентов, средний возраст)",
     "SELECT group_name, COUNT(*) as student_count, AVG(age) as avg_age FROM students GROUP BY group_name"),
    
    ("6. Поиск студентов по диапазону возраста (19-21 год)",
     "SELECT first_name, last_name, age, group_name FROM students WHERE age BETWEEN 19 AND 21 ORDER BY age DESC"),
]

# Выполняем все запросы
if __name__ == "__main__":
    print("🎓 АНАЛИТИКА СТУДЕНТОВ УНИВЕРСИТЕТА")
    print("База данных: university.db")
    
    for title, sql in queries:
        print_query_result(title, sql)
    
    # Дополнительно: интерактивный запрос
    print("\n" + "="*50)
    print("ВЫПОЛНИТЕ СВОЙ ЗАПРОС")
    print("Примеры:")
    print("  SELECT * FROM students WHERE age > 20 AND group_name = 'ИТ-101'")
    print("  SELECT first_name, last_name FROM students ORDER BY last_name LIMIT 5")
    
    while True:
        user_query = input("\nВведите SQL-запрос (или 'exit' для выхода): ").strip()
        
        if user_query.lower() == 'exit':
            break
        
        if user_query.upper().startswith('SELECT'):
            try:
                print_query_result("Ваш запрос", user_query)
            except Exception as e:
                print(f"Ошибка: {e}")
        else:
            print("Пожалуйста, используйте только SELECT-запросы для безопасности")
```

**Что нужно сделать студентам:**

1. **Установите библиотеку** для красивого вывода:
```bash
pip install tabulate
```

2. **Сохраните скрипт** как `student_queries.py` в той же папке, где находится `university.db`

3. **Запустите скрипт** и проанализируйте вывод:
```bash
python student_queries.py
```

4. **Модифицируйте скрипт**, добавив свои запросы:
   - Найдите всех студентов, чьё имя начинается на "А"
   - Найдите 3 самых молодых студента из группы "Л-201"
   - Найдите студентов, у которых возраст не равен 20

5. **Создайте новый скрипт** `book_search.py` для базы `library.db` из предыдущей лекции, который будет:
   - Искать книги по автору
   - Показывать книги, изданные после определённого года
   - Выводить список уникальных жанров
   - Сортировать книги по году издания (сначала новые)

#### **Итоги и практическое задание**

**Краткий итог:**
- `SELECT` — инструмент чтения данных
- `WHERE` фильтрует строки по условиям
- `ORDER BY` сортирует результаты
- `LIMIT` ограничивает количество записей
- SQL-запросы легко интегрируются в Python-скрипты

**Практическое задание на оценку:**

Создайте Python-скрипт `university_report.py`, который генерирует отчёт по студентам и **сохраняет его в файл `report.txt`**. Скрипт должен:

1. Подключаться к базе `university.db`
2. Выполнять как минимум 5 различных SELECT-запросов с использованием:
   - `WHERE` с `AND`/`OR`
   - `ORDER BY` с сортировкой по разным полям
   - `LIMIT` с `OFFSET`
   - Операторов сравнения (`>`, `<`, `=`, `!=`)
   - `LIKE` для поиска по шаблону
3. Для каждого запроса выводить:
   - Текст SQL-запроса
   - Количество найденных записей
   - Сами записи в читаемом виде
4. В конце отчёта добавить статистику:
   - Общее количество студентов
   - Количество студентов в каждой группе
   - Средний возраст по университету
   - Самого старшего и самого младшего студента

**Пример вывода в файле:**
```
ОТЧЁТ ПО СТУДЕНТАМ УНИВЕРСИТЕТА
Дата: 2023-10-15

1. ЗАПРОС: Студенты ИТ-101 старше 20 лет
   SQL: SELECT * FROM students WHERE group_name = 'ИТ-101' AND age > 20
   Найдено: 2 студента
   Результат:
   | id | first_name | last_name | age | group_name |
   |----|------------|-----------|-----|------------|
   | 2  | Петр       | Смирнов   | 22  | ИТ-101     |
   | 10 | Сергей     | Петров    | 22  | ИТ-101     |

...

СТАТИСТИКА:
Всего студентов: 10
По группам:
  ИТ-101: 5 студентов
  Л-201: 3 студента
  Ф-301: 2 студента
Средний возраст: 20.8 лет
Самый старший: Дмитрий Новиков (24 года)
Самый младший: Алексей Лебедев (18 лет)
```