### **Лекция 5.1: Введение в реляционные БД. SQLite.**

**Цель лекции:** Понять базовые концепции реляционных баз данных, освоить установку и первое подключение к SQLite, а также создать первую таблицу.

#### **Теоретическая часть**

**1. Что такое реляционная база данных (БД)?**
Это структурированный набор данных, организованный в виде **таблиц**, между которыми могут быть установлены связи (*relations*). Данные хранятся строго и предсказуемо, что обеспечивает их целостность и минимизирует дублирование.

**2. Ключевые концепции:**
*   **Таблица (Table / Relation):** Основной объект БД. Представляет собой набор строк и столбцов (как лист Excel). Каждая таблица описывает одну сущность (например, `Студенты`, `Книги`, `Заказы`).
*   **Столбец (Column / Field / Attribute):** Определяет тип данных и имя для характеристики сущности. Например, для таблицы `Студенты` столбцы могут быть: `id`, `имя`, `фамилия`, `возраст`. Каждый столбец имеет строгий тип данных (`INTEGER`, `TEXT`, `DATE` и т.д.).
*   **Строка (Row / Record / Tuple):** Это один конкретный экземпляр данных в таблице. Например, одна строка в таблице `Студенты` содержит данные одного студента: `(1, 'Иван', 'Петров', 20)`.
*   **Первичный ключ (PRIMARY KEY):** Это специальный столбец (или комбинация столбцов), который **уникально идентифицирует каждую строку** в таблице.
    *   Значение первичного ключа **не может повторяться** и **не может быть `NULL`**.
    *   Чаще всего это столбец с именем `id` и типом `INTEGER`. В SQLite такой столбец, объявленный как `INTEGER PRIMARY KEY`, автоматически увеличивается (AUTOINCREMENT) при добавлении новых строк.

**3. Введение в SQLite**
*   **Что это?** SQLite — это встраиваемая, легковесная, автономная реляционная СУБД. Весь движок базы данных содержится в одной библиотеке, а сама БД — в одном обычном файле на диске (например, `database.db`).
*   **Преимущества для обучения:** Не требует сложной установки сервера, идеальна для прототипирования, мобильных приложений и обучения SQL.
*   **Язык запросов:** Использует стандартный **SQL (Structured Query Language)** для взаимодействия с данными.

#### **Практическая часть: Установка и первые шаги**

**Задание 1: Установка SQLite и инструментов**

1.  **Скачайте и установите DB Browser for SQLite (DB4S):**
    *   Это бесплатный графический инструмент для работы с SQLite.
    *   Перейдите на [sqlitebrowser.org](https://sqlitebrowser.org/), скачайте версию для вашей ОС и установите.
    *   *Альтернатива (для продвинутых):* Установите сам `sqlite3` через терминал (`sudo apt install sqlite3` на Linux, или он уже предустановлен на macOS; для Windows скачайте с sqlite.org). Но DB4S удобнее для визуализации.

**Задание 2: Создание первой базы данных и таблицы**

1.  **Создайте новую БД:**
    *   Откройте DB Browser for SQLite.
    *   Нажмите **"Новая база данных"**.
    *   Выберите папку для сохранения и назовите файл `university.db`. Нажмите "Сохранить".

2.  **Создайте таблицу `students` через вкладку "Выполнить SQL":**
    *   Перейдите на вкладку **"Выполнить SQL"**.
    *   Вставьте следующий SQL-запрос. Проанализируйте его структуру:

    ```sql
    CREATE TABLE students (
        id INTEGER PRIMARY KEY,
        first_name TEXT NOT NULL,
        last_name TEXT NOT NULL,
        age INTEGER,
        group_name TEXT
    );
    ```
    *   **Объяснение запроса:**
        *   `CREATE TABLE students` — команда создания таблицы с именем `students`.
        *   `( ... )` — внутри скобок перечисляются столбцы.
        *   `id INTEGER PRIMARY KEY` — создаёт столбец-первичный ключ с автоинкрементом.
        *   `first_name TEXT NOT NULL` — столбец для текста, который не может быть пустым (`NOT NULL`).
        *   `age INTEGER` — столбец для целого числа, может быть пустым.
    *   Нажмите кнопку **"Выполнить запрос"** (или `Ctrl+R`).

3.  **Проверьте структуру таблицы:**
    *   Перейдите на вкладку **"Структура базы данных"**.
    *   Убедитесь, что ваша таблица `students` появилась в списке. Разверните её, чтобы увидеть созданные столбцы. Иконка золотого ключа (`🔑`) рядом с `id` указывает на первичный ключ.

**Задание 3: Визуальное добавление данных**

1.  **Добавьте данные через интерфейс:**
    *   Перейдите на вкладку **"Данные"**.
    *   В выпадающем списке выберите таблицу `students`.
    *   Нажмите кнопку **"Добавить запись"**.
    *   Вручную заполните поля для 2-3 студентов. **Обратите внимание:** Поле `id` можно оставить пустым — SQLite автоматически подставит следующее число.
    *   Нажмите **"Сохранить изменения"** и подтвердите (`OK`).

2.  **Просмотрите введённые данные:**
    *   Данные появятся в виде таблицы на вкладке "Данные". Каждая строка — это запись в таблице `students`.

#### **Итоги и домашнее задание**

**Краткий итог:**
*   Реляционная БД состоит из **таблиц**, которые содержат **столбцы** и **строки**.
*   **Первичный ключ** (`PRIMARY KEY`) уникально идентифицирует запись.
*   **SQLite** — простая в использовании файловая СУБД, идеальная для начала работы.
*   **DB Browser for SQLite** — удобный графический клиент для работы с SQLite.

**Домашнее задание:**

1.  **Создайте свою базу данных** `library.db`.
2.  **Спроектируйте и создайте** таблицу `books` со следующими столбцами:
    *   `book_id` — целочисленный первичный ключ.
    *   `title` — текстовое поле для названия книги (обязательное).
    *   `author` — текстовое поле для автора (обязательное).
    *   `year_published` — целое число для года издания.
    *   `genre` — текстовое поле для жанра.
3.  **Добавьте через интерфейс DB4S** как минимум 5 различных книг.
4.  **Сделайте скриншот** вкладки "Данные" с вашей таблицей `books` и сохраните его. Это будет результат выполнения домашнего задания.